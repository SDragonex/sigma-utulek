<Window x:Class="cigan_dialog.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cigan_dialog"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Content="Chyba"
                Margin="10"
                Background="Red"
                Click="Chyba_Click"/>

        <Button Content="Otázka"
                Margin="10"
                Background="CadetBlue"
                Click="Otazka_Click"
                Grid.Row="1"/>

        <Button Content="Upozornění"
                Margin="10"
                Background="Yellow"
                Click="Upozorneni_Click"
                Grid.Row="2"/>

        <Button Content="Informace"
                Margin="10"
                Background="LightSteelBlue"
                Click="Informace_Click"
                Grid.Row="3"/>
    </Grid>
</Window>
